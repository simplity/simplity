<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<record
  xmlns="http://www.simplity.org/schema"
  name="student" 
  moduleName="user"
  recordType="storage"
  description="all student attributes, other than the ones stored in user. primary key is same as the one in user"
>
  <fields>
    <primaryKey
      name="studentId"
      dataType="id"
      description="this is equal to userId in user table." />
  </fields>
  
</record>